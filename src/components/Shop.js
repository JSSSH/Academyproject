import { useNavigate } from "react-router-dom";
import '../css/Shop.css';

const Shop = () => {
  const navigate = useNavigate();

  return (
    <div className="Shop">
      
    </div>
  );
}

export default Shop;